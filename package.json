{
  "name": "stockscope-vercel",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:web": "cd frontend && npm install && npx expo export --platform web"
  },
  "dependencies": {
    "axios": "^1.6.5",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "node-cache": "^5.1.2"
  }
}
